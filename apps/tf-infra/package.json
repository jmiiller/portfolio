{
  "name": "@portfolio/tf-infra",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "description": "Terraform infrastructure for remote state storage",
  "scripts": {
    "deploy": "cd terraform && ./deploy.sh",
    "tf-format": "cd terraform && terraform fmt -recursive",
    "clean": "cd terraform && rimraf .terraform *.tfplan *.tfstate*"
  },
  "devDependencies": {},
  "keywords": [
    "terraform",
    "infrastructure",
    "aws",
    "s3",
    "dynamodb",
    "state-management"
  ]
}
